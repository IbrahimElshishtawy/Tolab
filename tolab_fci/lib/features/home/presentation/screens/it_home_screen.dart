import 'package:flutter/material.dart';

class ItHomeScreen extends StatelessWidget {
  const ItHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('IT Home')));
  }
}
